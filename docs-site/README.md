# DEPRECATED

This directory is **deprecated** and no longer maintained.

The canonical documentation lives in the top-level [`docs/`](../docs/) directory.

## Migration Status

All unique content has been migrated:
- `guides/log-forwarding.md` → [`docs/guides/log-forwarding.md`](../docs/guides/log-forwarding.md)

Static assets (favicon, icons, logo, demo video) are retained here for the legacy deployed site.
These should be migrated to a proper static assets location before this directory is removed.

## Replacements

- **Raw documentation**: [`docs/`](../docs/) — all architecture, security, ops, and compliance docs
- **Web docs site**: [`vitepress-docs/`](../vitepress-docs/) — VitePress-powered documentation website (migration in progress)

Do not add new content here.
