🔒 AgentGuard has been deployed!

{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}

  Dashboard & API: https://{{ $host.host }}
  Health check:    https://{{ $host.host }}/health

{{- end }}
{{- else }}

  Get the application URL by running:

  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "agentguard.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  kubectl port-forward --namespace {{ .Release.Namespace }} $POD_NAME 8080:{{ .Values.config.port }}

  Then visit: http://localhost:8080

{{- end }}

  Verify health:
  kubectl exec -it deploy/{{ include "agentguard.fullname" . }} -- curl -s http://localhost:{{ .Values.config.port }}/health

{{- if not .Values.secrets.licenseKey }}

  ℹ️  Running on FREE tier (no license key set).
  Upgrade at https://agentguard.tech/pricing
{{- end }}

  📖 Docs: https://docs.agentguard.tech
